import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GhostPDF - Share PDFs That Disappear",
  description:
    "Upload a PDF, get a secure link. The document self-destructs after first view or 10 minutes. No accounts. No traces. End-to-end encrypted.",
  openGraph: {
    title: "GhostPDF - Share PDFs That Disappear",
    description:
      "Upload a PDF, get a secure link. The document self-destructs after first view or 10 minutes.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-zinc-950 text-zinc-100">
        {children}
      </body>
    </html>
  );
}
